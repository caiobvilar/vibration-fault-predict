# docs/requirements/template.yaml
# Sample requirement for the template repo itself. Project repos replace this
# file's contents with their own requirements before writing code.
- id: TPL-FUN-001
  text: >
    The domain placeholder shall return the value 42 when invoked.
  type: functional
  rationale: >
    N-01 requires the template be host-testable from the first commit. The
    placeholder gives the L1 unit-test pipeline a real target so the template
    repo is green before any project work begins.
  parent: N-01
  priority: shall
  verification: test
  test_case: [test/unit/test_domain.c]
  status: approved
