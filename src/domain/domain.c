/* src/domain/domain.c -- placeholder for host-testable domain logic.
 *
 * Projects replace this with their pure algorithms. It must stay free of
 * hardware includes (tools/check_layering.py enforces this). Exists so the
 * template's host build is green from the first commit.
 */
#include "domain.h"

int domain_placeholder(void) { return 42; }
